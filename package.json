{
  "name": "kenyan-headline-vault",
  "version": "0.0.1",
  "description": "Kenyan News Archiving Website.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx eslint . test",
    "lint:fix": "npm run lint -- --fix",
    "prettier": "npx prettier . test --check",
    "prettier:fix": "npm run prettier -- --write",
    "format": "npm run prettier:fix && npm run lint:fix",
    "start": "node index.js",
    "start:dev": "NODE_ENV=development nodemon index.js",
    "serve": "NODE_ENV=development nodemon",
    "serve:inspect": "NODE_ENV=development nodemon --inspect ./index.js 4000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^5.7.0",
    "mongoose": "^7.4.0",
    "puppeteer": "^20.5.0"
  },
  "devDependencies": {
    "eslint": "^8.43.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.22",
    "prettier": "^2.8.8"
  }
}
